<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réservations - Port de plaisance de Russell</title>
</head>

<body>
    <header>
        <h1>Gestion des réservations - Port de plaisance de Russel</h1>
        <nav>
            <ul>
                <li><a href="/dashboard">Retour au tableau de bord</a></li>
                <li><a href="/auth/logout">Déconnexion</a></li>
            </ul>
        </nav>
    </header>

    <main>

        <button onclick="window.location.href='/reservations/add'">Ajouter une réservation</button>        

        <table>
            
            <caption>Liste des réservations en cours</caption>

            <thead>
                <tr>
                    <th>Numéro de catway</th>
                    <th>Nom du client</th>
                    <th>Nom du bateau</th>
                    <th>Date de début</th>
                    <th>Date de fin</th>
                </tr>
            </thead>

            <tbody>
                <% reservations.forEach(function(reservation) { %>
                    <tr>
                        <td><%= reservation.catwayNumber %></td>
                        <td><%= reservation.clientName %></td>
                        <td><%= reservation.boatName %></td>
                        <td><time datetime="<%= reservation.startDate %>"><%= new Date(reservation.startDate).toLocaleDateString('fr-FR') %></time></td>
                        <td><time datetime="<%= reservation.endDate %>"><%= new Date(reservation.endDate).toLocaleDateString('fr-FR') %></time></td> 
                    </tr>
                <% }); %>
            </tbody>
        </table>


    </main>

    <footer>
        <small>Vous pourrez trouver la documentation de l'API ici : <a href="#">Documentation de l'API</a></small>
    </footer>

</body>
</html>