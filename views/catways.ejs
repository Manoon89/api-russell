<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catways - Port de plaisance de Russell</title>
    <meta name="robots" content="noindex">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <header>
        <h1 class="text-center">Gestion des utilisateurs - Port de plaisance de Russel</h1>
        <nav class="navbar navbar-expand-lg bg-body-tertiary border mb-4 mt-4">
            <div class="container-fluid">
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link bg-dark-subtle active m-2 text-dark-emphasis" aria-current="page" href="/dashboard">Retour au tableau de bord</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link bg-dark-subtle active m-2 text-dark-emphasis" aria-current="page" href="/auth/logout">Déconnexion</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main>

        <div class="text-center m-4">
            <% if (error) { %>
                <div><%= error %></div>
            <% } %>
            <% if (success) { %>
                <div><%= success %></div>
            <% } %>
        </div>
        
        <div class="d-flex justify-content-center">
            <button type="button" class="btn btn-primary" onclick="window.location.href='/catways/add'">Ajouter un catway</button>   
        </div>

        <table class="table m-4 table-bordered">
            
            <caption>Liste des catways du port</caption>

            <thead>
                <tr>
                    <th scope="col">Numéro de catway</th>
                    <th scope="col">Type de catway</th>
                    <th scope="col">Etat du catway</th>
                    <th scope="col">Modifier / Supprimer</th>
                </tr>
            </thead>

            <tbody>
                <% catways.forEach(function(catway) { %>
                    <tr>
                        <td class="align-middle"><%= catway.catwayNumber %></td>
                        <td class="align-middle"><%= catway.catwayType %></td>
                        <td class="align-middle"><%= catway.catwayState %></td>
                        <td>
                            
                            <button type="button" class="d-inline m-2 p-2 btn btn-outline-primary btn-sm" onclick="window.location.href='/catways/<%= catway._id %>'">Détails</button>
                            
                            <button type="button" class="d-inline m-2 p-2 btn btn-outline-primary btn-sm" onclick="window.location.href='/catways/edit/<%= catway._id %>'">Modifier</button>
                            
                            <form class="d-inline" action="/catways/<%= catway._id %>?_method=DELETE" method="POST">
                                <button class="d-inline m-2 p-2 btn btn-outline-primary btn-sm" type="submit" onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce catway ?')">Supprimer</button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

    </main>

    <footer class="text-center">
        <small>Vous pourrez trouver la documentation de l'API ici : <a href="http://localhost:8080/api-docs/">Documentation de l'API</a></small>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>