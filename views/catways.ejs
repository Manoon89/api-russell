<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catways - Port de plaisance de Russell</title>
</head>

<body>
    <header>
        <h1>Gestion des catways - Port de plaisance de Russel</h1>
        <nav>
            <ul>
                <li><a href="/dashboard">Retour au tableau de bord</a></li>
                <li><a href="/auth/logout">Déconnexion</a></li>
            </ul>
        </nav>
    </header>

    <main>

        <% if (error) { %>
            <div><%= error %></div>
        <% } %>
        <% if (success) { %>
            <div><%= success %></div>
        <% } %>
        
        <button onclick="window.location.href='/catways/add'">Ajouter un catway</button>   

        <table>
            
            <caption>Liste des catways du port</caption>

            <thead>
                <tr>
                    <th>Numéro de catway</th>
                    <th>Type de catway</th>
                    <th>Etat du catway</th>
                    <th>Modifier / Supprimer</th>
                </tr>
            </thead>

            <tbody>
                <% catways.forEach(function(catway) { %>
                    <tr>
                        <td><%= catway.catwayNumber %></td>
                        <td><%= catway.catwayType %></td>
                        <td><%= catway.catwayState %></td>
                        <td>
                            
                            <button onclick="window.location.href='/catways/<%= catway._id %>'">Détails</button>
                            
                            <button onclick="window.location.href='/catways/edit/<%= catway._id %>'">Modifier</button>
                            
                            <form action="/catways/<%= catway._id %>?_method=DELETE" method="POST">
                                <button type="submit" onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce catway ?')">Supprimer</button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

    </main>

    <footer>
        <small>Vous pourrez trouver la documentation de l'API ici : <a href="../docs/index.html">Documentation de l'API</a></small>
    </footer>

</body>
</html>